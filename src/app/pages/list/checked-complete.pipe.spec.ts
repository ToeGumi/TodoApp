import { CheckedCompletePipe } from './checked-complete.pipe';

describe('CheckedCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
